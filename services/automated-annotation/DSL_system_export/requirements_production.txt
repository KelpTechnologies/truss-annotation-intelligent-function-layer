# PRODUCTION REQUIREMENTS - Lambda/Cloud Deployment
# =================================================
#
# This file contains ONLY the dependencies needed for production deployment.
# Use this instead of requirements.txt when deploying to Lambda.
#
# Install with: pip install -r requirements_production.txt

# Core LLM dependencies
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-google-vertexai>=2.0.0,<3.0.0
pydantic>=2.0.0,<3.0.0

# Data processing
pandas>=1.5.0

# HTTP requests for DSL API
requests>=2.31.0

# Image processing for base64 encoding
Pillow>=9.0.0

# Note: python-dotenv is NOT included - use Lambda environment variables instead
