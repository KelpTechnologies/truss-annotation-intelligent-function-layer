# Test Environment Variables
# Copy to .env and fill in values

# =============================================================================
# API MODE - Required for --mode api
# =============================================================================
STAGING_API_URL=https://your-staging-api-url.com
DSL_API_KEY=your-api-key

# =============================================================================
# LOCAL MODE - Required for --mode local
# =============================================================================

# AWS Configuration (for DynamoDB config loading)
AWS_REGION=eu-west-2
AWS_PROFILE=your-aws-profile  # Or use AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY

# Secrets ARN (contains GCP creds, Pinecone key, etc.)
TRUSS_SECRETS_ARN=arn:aws:secretsmanager:eu-west-2:123456789:secret:truss-secrets

# Environment/Stage
STAGE=staging

# GCP/Vertex AI (for LLM calls)
VERTEXAI_PROJECT=truss-data-science
VERTEXAI_LOCATION=us-central1

# Alternative: Direct GCP credentials (if not using TRUSS_SECRETS_ARN)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCP_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Alternative: Direct Pinecone key (if not using TRUSS_SECRETS_ARN)
# PINECONE_API_KEY=your-pinecone-key
